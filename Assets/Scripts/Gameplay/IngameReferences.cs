using UnityEngine;

/*
 * Maybe this is a weird design decision, but I thought the simplest thing is probably when we just store all references every Driver needs to access
 * in a static place instead of providing them to every single driver. 
 * So here we go, ask us anything. 
 */

public class IngameReferences
{

}
